# --------------------------------------------------------------------------
# Server Configuration
# --------------------------------------------------------------------------
PORT=3000
NODE_ENV=development

# --------------------------------------------------------------------------
# Security & Authentication
# --------------------------------------------------------------------------
# Your personal phone number that has "God Mode" access (full commands)
# Format: Country code + number without plus sign (e.g., 254712345678)
OWNER_PHONE_NUMBER=

# --------------------------------------------------------------------------
# Database Connection (Neon / Postgres)
# --------------------------------------------------------------------------
# Required for:
# 1. Storing WhatsApp session data (prevents QR code loop)
# 2. Saving contact memory & trust scores
# 3. Message logging for recent activity
#
# Get your free instance: https://console.neon.tech/
# Format: postgres://<user>:<password>@<host>/<dbname>?sslmode=require
DATABASE_URL=postgres://user:pass@ep-cool-db-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# --------------------------------------------------------------------------
# Artificial Intelligence (Google Gemini)
# --------------------------------------------------------------------------
# The model that powers the agent's logic.
# Recommended: 'gemini-2.5-flash' (Fast & Capable) or 'gemini-1.5-flash'
GEMINI_MODEL=gemini-2.5-flash

# API KEYS
# You can provide a single key or multiple keys to handle rate limits.
# Get keys here: https://makersuite.google.com/app/apikey

# Option 1: Single Key (easiest, but may hit rate limits)
GEMINI_API_KEY=AIzaSy...

# Option 2: Key Pool (Recommended for high volume)
# The agent will rotate through these keys if one gets rate-limited.
GEMINI_API_KEY1=AIzaSy...
GEMINI_API_KEY2=AIzaSy...
GEMINI_API_KEY3=AIzaSy...
# ... up to GEMINI_API_KEY50

# Option 3: Comma-separated list
# GEMINI_API_KEYS=key1,key2,key3

# --------------------------------------------------------------------------
# Google Calendar Scheduling (Optional)
# --------------------------------------------------------------------------
# Your calendar email (e.g., yourname@gmail.com)
# Required if you want customers to schedule meetings via WhatsApp
GOOGLE_CALENDAR_ID=

# Working hours for availability checking (24-hour format HH:MM)
WORKING_HOURS_START=09:00
WORKING_HOURS_END=18:00

# Minimum meeting duration in minutes
MIN_MEETING_DURATION=10

# Buffer time between meetings in minutes
BUFFER_TIME=15

# Days of week to allow bookings (comma-separated: 1=Mon, 2=Tue, ..., 7=Sun)
# Default: 1,2,3,4,5 (Monday to Friday)
BOOKING_DAYS=1,2,3,4,5
